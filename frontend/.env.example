# Frontend env
# Leave empty for local dev: Vite proxies /api → http://127.0.0.1:8000 (see vite.config.js).
# Production build: empty is OK when nginx serves /api on the same host.
# Set only if the API is on another origin, e.g. VITE_API_URL=https://api.example.com
VITE_API_URL=
