# Postgres credentials (used by the postgres Docker container).
# Use the same values in backend/.env as DB_NAME, DB_USER, DB_PASSWORD (with DB_HOST=db).
POSTGRES_DB=raffaello
POSTGRES_USER=raffaello_user
POSTGRES_PASSWORD=change-this-strong-password
